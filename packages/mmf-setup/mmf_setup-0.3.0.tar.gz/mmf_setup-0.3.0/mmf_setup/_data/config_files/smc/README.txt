This directory contains files for setting up a project on Sage Mathcloud
(cloud.sagemath.org).  They are based on the following assumptions:

1. That the default .bashrc file sources .bash_aliases.  We put all of our
   customizations in this latter file.
2.
