stages:
  - test
  - deploy

centos7:
  except:
    - tags
  image: gitlab-registry.cern.ch/lhcb-docker/rabbitmq-python-deployment:centos7
  script:
    - init.sh # For RabbitMQ Usage
    - python --version
    - pip install pycodestyle importlib nose coverage
    - pip install .
    - nosetests --with-coverage --cover-package=lbdevmanager --cover-html --with-xunit --exe
    - pycodestyle lbinstall/ > pep8_report.txt || true
    - mkdir -p cover_report && mv -f cover cover_report/$CI_JOB_NAME
    - mkdir -p pep8_report && mv -f pep8_report.txt pep8_report/$CI_JOB_NAME.txt
  artifacts:
    paths:
      - cover_report
      - pep8_report
    when: always
    expire_in: 1 week


python2.7:
  image: gitlab-registry.cern.ch/lhcb-docker/rabbitmq-python-deployment:python-2.7
  script:
    - init.sh # For RabbitMQ Usage
    - python --version
    - pip install pycodestyle importlib nose coverage
    - pip install .
    - nosetests --with-coverage --cover-package=lbdevmanager --cover-html --with-xunit --exe
    - pycodestyle lbinstall/ > pep8_report.txt || true
    - mkdir -p cover_report && mv -f cover cover_report/$CI_JOB_NAME
    - mkdir -p pep8_report && mv -f pep8_report.txt pep8_report/$CI_JOB_NAME.txt
    - python setup.py sdist --dist-dir public/

  artifacts:
    paths:
      - cover_report
      - pep8_report
      - public
    when: always
    expire_in: 1 week

python3.7:
  except:
    - tags
  image: gitlab-registry.cern.ch/lhcb-docker/rabbitmq-python-deployment:python-3.7
  script:
    - init.sh # For RabbitMQ Usage
    - python --version
    - pip install pycodestyle nose coverage
    - pip install .
    - nosetests --with-coverage --cover-package=lbdevmanager --cover-html --with-xunit --exe
    - pycodestyle lbinstall/ > pep8_report.txt || true
    - mkdir -p cover_report && mv -f cover cover_report/$CI_JOB_NAME
    - mkdir -p pep8_report && mv -f pep8_report.txt pep8_report/$CI_JOB_NAME.txt
  artifacts:
    paths:
      - cover_report
      - pep8_report
    when: always
    expire_in: 1 week

python3.6:
  except:
    - tags
  image: gitlab-registry.cern.ch/lhcb-docker/rabbitmq-python-deployment:python-3.6
  script:
    - init.sh # For RabbitMQ Usage
    - python --version
    - pip install pycodestyle nose coverage
    - pip install .
    - nosetests --with-coverage --cover-package=lbdevmanager --cover-html --with-xunit --exe
    - pycodestyle lbinstall/ > pep8_report.txt || true
    - mkdir -p cover_report && mv -f cover cover_report/$CI_JOB_NAME
    - mkdir -p pep8_report && mv -f pep8_report.txt pep8_report/$CI_JOB_NAME.txt
  artifacts:
    paths:
      - cover_report
      - pep8_report
    when: always
    expire_in: 1 week

deploy-packages:
  stage: deploy
  only:
    - tags
  dependencies:
    - python2.7
  image: gitlab-registry.cern.ch/lhcb-docker/python-deployment:python-3.7
  script:
    - if [ -z "$TWINE_PASSWORD" ] ; then echo "Set TWINE_PASSWORD in CI variables" ; exit 1 ; fi
    - twine upload -u __token__ public/*
  before_script: []
  after_script: []
