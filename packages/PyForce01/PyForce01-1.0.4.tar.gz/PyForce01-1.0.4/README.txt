Documentation:
    Version:
        This is v1.0.0 of PyForce

    Description:
        This is a module for bruteforcing district 833 school passwords using destiny quest.

    Usage:
	pip install PyForce
	
	Example:
	import PyForce
	PyForce.create_webdriver(username=<username>,browser_type='Chrome'#Prefered Browser but firefox works too if you the correct driver.#,driver_path=<driver path>)
	
	Then it will do its stuff, if there are any bugs contact me.