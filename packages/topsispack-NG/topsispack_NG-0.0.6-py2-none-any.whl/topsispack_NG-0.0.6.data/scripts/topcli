#!python

import sys
from topsispack_NG import topsis

sysarglist = sys.argv
sysarglist.pop(0)
assert len(sysarglist) == 3, "Insufficient number of arguments provided"

filename = sys.argv[1]
assert filename, "Filename must be provided"
topsis(sysarglist)
