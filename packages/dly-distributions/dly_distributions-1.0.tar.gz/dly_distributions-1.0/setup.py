from setuptools import setup

setup(name='dly_distributions',
      version='1.0',
      description='Gaussian/Binomial distributions',
      packages=['dly_distributions'],
      author= 'Derek Ly',
      zip_safe=False)
