#!/usr/bin/env python
import sys

from redraw._cli import demo

if __name__ == "__main__":
    if sys.version_info[0] == 3 and sys.version_info[1] >= 6:
        demo()
    else:
        raise Exception("Please use Python 3.6+")
