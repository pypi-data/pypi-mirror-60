web = dict(
    production = "web",
    staging = "staging"
)

data = dict(
    production = "data/data.json",
    staging = "data/data_draft.json",
    base = "data"
)

content_path = "content"
home_template = "home.html"
non_listed_types = ["page"]

production_port = 8000
staging_port = 8080