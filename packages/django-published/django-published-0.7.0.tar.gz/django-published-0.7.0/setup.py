#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['published', 'published.tests']

package_data = \
{'': ['*']}

install_requires = \
['django>=2.2']

extras_require = \
{':python_version == "3.6"': ['dataclasses'],
 'test': ['flake8', 'coverage', 'mock']}

setup(name='django-published',
      version='0.7.0',
      description='Control public visibility of model instances.',
      author='Luke Rogers',
      author_email='luke@dmptr.com',
      url='https://github.com/dmptrluke/django-published',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      python_requires='>=3.6',
     )
