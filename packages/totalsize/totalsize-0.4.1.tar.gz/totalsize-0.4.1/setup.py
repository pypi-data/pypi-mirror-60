#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['totalsize']

package_data = \
{'': ['*']}

install_requires = \
['youtube-dl >=2019.11.22']

entry_points = \
{'console_scripts': ['totalsize = totalsize.total:cli']}

setup(name='totalsize',
      version='0.4.1',
      description='Totalsize uses youtube-dl to calculate total size of all videos in a playlist',
      author='theychx',
      author_email='theychx@fastmail.com',
      url='https://github.com/theychx/totalsize',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
